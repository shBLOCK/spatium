name: Lint

on:
  push:
    paths:
      - "**.py"
      - "**.pyi"
  pull_request:
    paths:
      - "**.py"
      - "**.pyi"
  workflow_dispatch:

jobs:
  black:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: psf/black@stable
